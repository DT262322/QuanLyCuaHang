package controller;

import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;

import javafx.scene.control.TextField;
import javafx.scene.layout.BorderPane;
import javafx.scene.layout.Pane;
import javafx.stage.Modality;
import javafx.stage.Stage;
import model.Fxmlloader;

import java.io.IOException;

import javafx.event.ActionEvent;

import javafx.scene.control.TableView;

public class ProductController {
	@FXML
	private TableView tableView;
	@FXML
	private TextField txtPDLine;
	@FXML
	private TextField txtPDLineName;
	@FXML
	private Button btnDelete;
	@FXML
	private Button btnAdd;
	@FXML
	private Button btnUpdate;
	@FXML
	private Button btnEdit;
	@FXML
	private Button btnShowPD;
	@FXML
	private TextField txtcategoryID;
	@FXML
	private TextField txtOverview;
	@FXML
	private TextField txtSearch;
	@FXML
	private Button btnSearch;

	// Event Listener on Button[#btnDelete].onAction
	@FXML
	public void btnDelete_click(ActionEvent event) {
		// TODO Autogenerated
	}
	// Event Listener on Button[#btnAdd].onAction
	@FXML
	public void btnAdd_click(ActionEvent event) {
		// TODO Autogenerated
	}
	// Event Listener on Button[#btnUpdate].onAction
	@FXML
	public void btnUpdate_click(ActionEvent event) {
		// TODO Autogenerated
	}
	// Event Listener on Button[#btnEdit].onAction
	@FXML
	public void btnEdit_click(ActionEvent event) {
		// TODO Autogenerated
	}
	// Event Listener on Button[#btnShowPD].onAction
	@FXML
	public void btnShowPD_click(ActionEvent event) {
		 try {
		        FXMLLoader loader = new FXMLLoader(getClass().getResource("/view/ProductList.fxml"));
		        Parent root = loader.load();

		        Scene scene = new Scene(root);

		        // Add the path to your CSS file
		        String cssPath = getClass().getResource("/application/application.css").toExternalForm();
		        scene.getStylesheets().add(cssPath);

		        Stage stage = new Stage();

		        stage.initModality(Modality.APPLICATION_MODAL);
		        stage.setScene(scene);
		        stage.showAndWait();
		    } catch (IOException e) {
		        e.printStackTrace();
		    }
		 public void setSellProductController(SellProductController sellProductController) {
		        this.sellProductController = sellProductController;
		    }
		
//		Scene currentScene=(Scene)btnShowPD.getScene();
//		Parent currentParent=currentScene.getRoot();
//		BorderPane pane=(BorderPane)currentParent.getChildrenUnmodifiable().get(0);
//		Fxmlloader object=new Fxmlloader();
//		Pane view=object.getPage("ProductList");
//		pane.setCenter(view);
//		
				
	   	
    }
	
	// Event Listener on Button[#btnSearch].onAction
	@FXML
	public void btnSearch_click(ActionEvent event) {
		// TODO Autogenerated
	}
}
